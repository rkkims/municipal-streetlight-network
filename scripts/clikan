#!/usr/bin/env bash
set -euo pipefail
project_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$project_dir"
export CLIKAN_HOME="$project_dir/.clikan"
exec /home/ubuntu/.local/bin/clikan "$@"
